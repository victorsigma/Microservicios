
spring:
    application:
        name: auth-service

    cloud:
        config:
            enabled: true
            uri: http://localhost:8081

    jpa:
        database: sqlserver
        show-sql: false
        hibernate:
            ddl-auto: update

    datasource:
        # url: jdbc:sqlserver://localhost;database=microservicios;encrypt=true;trustServerCertificate=true
        url: jdbc:mysql://localhost:3306/microservicios
        username: Sigma
        password: linux123

    webmvc:
        cors:
            allowed-origins: "*"
            allowed-methods: "GET, POST, PUT, DELETE"
            allowed-headers: "Content-Type, Authorization"
jwt:
    secret: secret

admin-paths:
    paths:
        -
            uri: "/user"
            method: "POST"
        -
            uri: "/user/savecar/{1}[1-9]+[0-9]*"
            method: "POST"
        -
            uri: "/user/savebike/{1}[1-9]+[0-9]*"
            method: "POST"
